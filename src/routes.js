import React                   from 'react';
import { Route, IndexRoute }   from 'react-router';
import ClubLayoutRoot from '../src/views/clubLayout/Container';

export default (
  <Route name="app" component={ClubLayoutRoot} path="/">
  </Route>
);
